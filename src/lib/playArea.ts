// Counties that are in play: the ones that actually contain eligible stations.
// The map dims every other county (see play-area-overlay skill). Sourced from the
// active region so each map dims the right area.
export { IN_PLAY_COUNTIES } from '../data/regions'
